// script.js
let words = [], activeStage = 1, activeIndex = 0, activeList = [], canProceed = false;
// ... բոլոր ֆունկցիաները (initApp, renderStage1, և այլն) ...

// Մի մոռացիր վերջում կանչել initApp();
initApp();
